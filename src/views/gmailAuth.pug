extends base
block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(name="auth", type="text", placeholder="Authentication Code", required)
        input(type="submit", value="Next")